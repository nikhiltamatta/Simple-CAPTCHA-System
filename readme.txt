Java:

// You can integrate this concept into your Java web application.
// For Java web applications, the backend logic can be handled using servlets or Spring MVC.
// The logic will be similar to the PHP example, but you'll handle it in Java classes.


Python:

# For Python, you can use a web framework like Flask or Django to handle the backend logic.
# The logic will be similar to the PHP example, but you'll handle it in Python functions or views.


Note:

This is a basic implementation of a CAPTCHA system using the provided languages. 
It's important to note that this example is simplistic and may not provide robust security against automated bots. 
For a production-level CAPTCHA system, you would need to consider more advanced techniques and possibly integrate with CAPTCHA services like Google reCAPTCHA.
